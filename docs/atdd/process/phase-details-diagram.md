# Phase Details Diagram

> Generated by the `diagram-generator` agent from the prose docs in `docs/atdd/process/`. Overwritten on every run — do not edit by hand; edit the source docs and regenerate.

## Source docs

- `docs/atdd/process/at-cycle-conventions.md`
- `docs/atdd/process/at-green-system.md`
- `docs/atdd/process/at-red-dsl.md`
- `docs/atdd/process/at-red-system-driver.md`
- `docs/atdd/process/at-red-test.md`
- `docs/atdd/process/commit-confirmation.md`
- `docs/atdd/process/ct-cycle-conventions.md`
- `docs/atdd/process/ct-green-stubs.md`
- `docs/atdd/process/ct-red-dsl.md`
- `docs/atdd/process/ct-red-external-driver.md`
- `docs/atdd/process/ct-red-test.md`
- `docs/atdd/process/cycles.md`
- `docs/atdd/process/glossary.md`
- `docs/atdd/process/phase-progression.md`
- `docs/atdd/process/task-and-chore-cycles.md`

Cycle-level routing across these phases lives in [process-diagram.md](process-diagram.md).

## AT - RED - TEST Phase Detail

```mermaid
flowchart TD
    START([Enter AT - RED - TEST - WRITE])
    WRITE[Write all acceptance tests for ticket]
    STOP_WRITE_TESTS[STOP - HUMAN REVIEW — approve tests]
    COMPILE[Attempt to compile tests]
    COMPILE_OK{Compiles?}
    EXTEND_DSL[Extend DSL interfaces; add TODO: DSL prototypes]
    STOP_DSL[STOP - HUMAN REVIEW — approve DSL prototype changes]
    RUN[Run tests; verify runtime failure]
    DISABLE[Disable tests with reason AT - RED - TEST]
    COMMIT[COMMIT: Ticket AT - RED - TEST]
    STOP_END[STOP - ORCHESTRATOR — phase progression]

    START --> WRITE
    WRITE --> STOP_WRITE_TESTS
    STOP_WRITE_TESTS --> COMPILE
    COMPILE --> COMPILE_OK
    COMPILE_OK -->|Yes| RUN
    COMPILE_OK -->|No| EXTEND_DSL
    EXTEND_DSL --> STOP_DSL
    STOP_DSL --> RUN
    RUN --> DISABLE
    DISABLE --> COMMIT
    COMMIT --> STOP_END

    classDef effortNode fill:#cce5ff,stroke:#004085,stroke-width:2px
    class WRITE,EXTEND_DSL effortNode
    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_WRITE_TESTS,STOP_DSL humanReviewNode
```

## AT - RED - DSL Phase Detail

```mermaid
flowchart TD
    START([Enter AT - RED - DSL - WRITE])
    ENABLE[Enable tests disabled AT - RED - TEST]
    IMPL_DSL[Implement DSL — replace TODO: DSL prototypes]
    UPDATE_DRIVER_IFACE[Update Driver interfaces as needed]
    SET_FLAGS[Set External and System Driver Interface Changed flags]
    STOP_WRITE[STOP - HUMAN REVIEW — approve DSL, Driver interfaces, flags]
    DRIVER_CHANGED{Any Driver interface changed?}
    IMPL_DRIVERS_PROTOTYPE[Implement Driver prototypes — TODO: Driver]
    RUN[Run tests; verify runtime failure]
    DISABLE[Disable tests with reason AT - RED - DSL]
    NO_TEST_FILES[Verify no test files in changed files]
    COMMIT[COMMIT: Ticket AT - RED - DSL]
    STOP_END[STOP - ORCHESTRATOR — phase progression]

    START --> ENABLE
    ENABLE --> IMPL_DSL
    IMPL_DSL --> UPDATE_DRIVER_IFACE
    UPDATE_DRIVER_IFACE --> SET_FLAGS
    SET_FLAGS --> STOP_WRITE
    STOP_WRITE --> DRIVER_CHANGED
    DRIVER_CHANGED -->|Yes| IMPL_DRIVERS_PROTOTYPE
    DRIVER_CHANGED -->|No| RUN
    IMPL_DRIVERS_PROTOTYPE --> RUN
    RUN --> DISABLE
    DISABLE --> NO_TEST_FILES
    NO_TEST_FILES --> COMMIT
    COMMIT --> STOP_END

    classDef effortNode fill:#cce5ff,stroke:#004085,stroke-width:2px
    class IMPL_DSL,UPDATE_DRIVER_IFACE,IMPL_DRIVERS_PROTOTYPE effortNode
    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_WRITE humanReviewNode
```

## AT - RED - SYSTEM DRIVER Phase Detail

```mermaid
flowchart TD
    START([Enter AT - RED - SYSTEM DRIVER - WRITE])
    ENABLE[Enable tests disabled AT - RED - DSL]
    IMPL[Implement System Drivers — replace TODO: Driver prototypes]
    RUN[Run tests; verify runtime failure]
    STOP_WRITE[STOP - HUMAN REVIEW — approve Driver implementation]
    DISABLE[Disable tests with reason AT - RED - SYSTEM DRIVER]
    NO_TEST_FILES[Verify no test files in changed files]
    COMMIT[COMMIT: Ticket AT - RED - SYSTEM DRIVER]
    STOP_END[STOP - ORCHESTRATOR — phase progression]

    START --> ENABLE
    ENABLE --> IMPL
    IMPL --> RUN
    RUN --> STOP_WRITE
    STOP_WRITE --> DISABLE
    DISABLE --> NO_TEST_FILES
    NO_TEST_FILES --> COMMIT
    COMMIT --> STOP_END

    classDef effortNode fill:#cce5ff,stroke:#004085,stroke-width:2px
    class IMPL effortNode
    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_WRITE humanReviewNode
```

## AT - GREEN - SYSTEM Phase Detail

```mermaid
flowchart TD
    START([Enter AT - GREEN - SYSTEM - WRITE])
    ENABLE[Enable tests disabled AT - RED - SYSTEM DRIVER]
    BACKEND[Implement backend changes]
    RUN_API[Run acceptance-api tests]
    API_PASS{API tests pass?}
    FIX_BACKEND[Fix backend]
    FRONTEND[Implement frontend changes]
    RUN_UI[Run acceptance-ui tests]
    UI_PASS{UI tests pass?}
    FIX_FRONTEND[Fix frontend]
    STOP_WRITE[STOP - HUMAN REVIEW — approve implementation]
    COMMIT[COMMIT: Ticket AT - GREEN - SYSTEM]
    TICK[Tick AC checkboxes; move issue to DONE if complete]
    END_NODE([Phase complete])

    START --> ENABLE
    ENABLE --> BACKEND
    BACKEND --> RUN_API
    RUN_API --> API_PASS
    API_PASS -->|No| FIX_BACKEND
    FIX_BACKEND --> RUN_API
    API_PASS -->|Yes| FRONTEND
    FRONTEND --> RUN_UI
    RUN_UI --> UI_PASS
    UI_PASS -->|No| FIX_FRONTEND
    FIX_FRONTEND --> RUN_UI
    UI_PASS -->|Yes| STOP_WRITE
    STOP_WRITE --> COMMIT
    COMMIT --> TICK
    TICK --> END_NODE

    classDef effortNode fill:#cce5ff,stroke:#004085,stroke-width:2px
    class BACKEND,FRONTEND,FIX_BACKEND,FIX_FRONTEND effortNode
    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_WRITE humanReviewNode
```

## CT - RED - TEST Phase Detail

```mermaid
flowchart TD
    START([Enter CT - RED - TEST - WRITE])
    WRITE[Write External System Contract Tests]
    RUN_REAL[Run against Real External System Test Instance]
    REAL_PASS{Pass against Real?}
    ASK_STOP[Ask user for support; STOP]
    RUN_STUB[Run against Stub External System; verify fails]
    DISABLE[Disable tests with reason CT - RED - TEST]
    STOP_WRITE[STOP - HUMAN REVIEW — approve contract tests]
    COMPILE_ERR{Compile errors during WRITE?}
    EXTEND_DSL[Extend DSL; throw TODO: DSL; verify runtime failure]
    COMMIT[COMMIT: Scenario CT - RED - TEST]
    STOP_END[STOP - ORCHESTRATOR — phase progression]

    START --> WRITE
    WRITE --> RUN_REAL
    RUN_REAL --> REAL_PASS
    REAL_PASS -->|No| ASK_STOP
    REAL_PASS -->|Yes| RUN_STUB
    RUN_STUB --> DISABLE
    DISABLE --> STOP_WRITE
    STOP_WRITE --> COMPILE_ERR
    COMPILE_ERR -->|Yes| EXTEND_DSL
    COMPILE_ERR -->|No| COMMIT
    EXTEND_DSL --> COMMIT
    COMMIT --> STOP_END

    classDef effortNode fill:#cce5ff,stroke:#004085,stroke-width:2px
    class WRITE effortNode
    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_WRITE humanReviewNode
```

## CT - RED - DSL Phase Detail

```mermaid
flowchart TD
    START([Enter CT - RED - DSL - WRITE])
    ENABLE[Enable tests disabled CT - RED - TEST]
    IMPL_DSL[Implement DSL — replace TODO: DSL stub]
    UPDATE_DRIVER_IFACE[Update Driver interfaces as needed]
    SET_FLAG[Set External System Driver Interface Changed flag]
    STOP_WRITE[STOP - HUMAN REVIEW — approve DSL, Driver interfaces, flag]
    IMPL_DRIVERS[Implement Drivers — throw TODO: Driver]
    RUN[Run tests; verify runtime failure]
    DISABLE[Disable tests with reason CT - RED - DSL]
    COMMIT[COMMIT: Scenario CT - RED - DSL]
    POST_COMMENT[Post issue comment summarising DSL interface changes]
    NEXT([Proceed to CT - RED - EXTERNAL DRIVER - WRITE])

    START --> ENABLE
    ENABLE --> IMPL_DSL
    IMPL_DSL --> UPDATE_DRIVER_IFACE
    UPDATE_DRIVER_IFACE --> SET_FLAG
    SET_FLAG --> STOP_WRITE
    STOP_WRITE --> IMPL_DRIVERS
    IMPL_DRIVERS --> RUN
    RUN --> DISABLE
    DISABLE --> COMMIT
    COMMIT --> POST_COMMENT
    POST_COMMENT --> NEXT

    classDef effortNode fill:#cce5ff,stroke:#004085,stroke-width:2px
    class IMPL_DSL,UPDATE_DRIVER_IFACE effortNode
    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_WRITE humanReviewNode
```

## CT - RED - EXTERNAL DRIVER Phase Detail

```mermaid
flowchart TD
    START([Enter CT - RED - EXTERNAL DRIVER - WRITE])
    ENABLE[Enable tests disabled CT - RED - DSL]
    IMPL[Implement external Drivers — replace TODO: Driver stub]
    RUN[Run tests; verify runtime failure]
    STOP_WRITE[STOP - HUMAN REVIEW — approve Driver implementation]
    DISABLE[Disable tests with reason CT - RED - EXTERNAL DRIVER]
    COMMIT[COMMIT: Scenario CT - RED - EXTERNAL DRIVER]
    POST_COMMENT[Post issue comment summarising Driver interface changes]
    STOP_END[STOP - ORCHESTRATOR — phase progression]

    START --> ENABLE
    ENABLE --> IMPL
    IMPL --> RUN
    RUN --> STOP_WRITE
    STOP_WRITE --> DISABLE
    DISABLE --> COMMIT
    COMMIT --> POST_COMMENT
    POST_COMMENT --> STOP_END

    classDef effortNode fill:#cce5ff,stroke:#004085,stroke-width:2px
    class IMPL effortNode
    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_WRITE humanReviewNode
```

## CT - GREEN - STUBS Phase Detail

```mermaid
flowchart TD
    START([Enter CT - GREEN - STUBS - WRITE])
    ENABLE[Enable tests disabled CT - RED - EXTERNAL DRIVER]
    IMPL_STUBS[Implement External System Stubs]
    RUN[Run External System Contract Tests against Stub]
    PASS{Tests pass?}
    ASK_STOP[Ask user for support; STOP]
    STOP_WRITE[STOP - HUMAN REVIEW — approve stub implementation]
    REMOVE_DISABLED[Remove CT - RED - EXTERNAL DRIVER disabled annotation]
    RUN_FINAL[Run tests; verify pass]
    COMMIT[COMMIT: Scenario CT - GREEN - STUBS]
    STOP_END[STOP - ORCHESTRATOR — phase progression]

    START --> ENABLE
    ENABLE --> IMPL_STUBS
    IMPL_STUBS --> RUN
    RUN --> PASS
    PASS -->|No| ASK_STOP
    PASS -->|Yes| STOP_WRITE
    STOP_WRITE --> REMOVE_DISABLED
    REMOVE_DISABLED --> RUN_FINAL
    RUN_FINAL --> COMMIT
    COMMIT --> STOP_END

    classDef effortNode fill:#cce5ff,stroke:#004085,stroke-width:2px
    class IMPL_STUBS effortNode
    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_WRITE humanReviewNode
```
