# Process Diagram

> Generated by the `diagram-generator` agent from the prose docs in `docs/atdd/process/` and the cycle-level orchestration YAML in `docs/atdd/process/process-flow.yaml`. Overwritten on every run — do not edit by hand; edit the source docs and regenerate.

## Source docs

- `docs/atdd/process/process-flow.yaml`
- `docs/atdd/process/at-cycle-conventions.md`
- `docs/atdd/process/at-green-system.md`
- `docs/atdd/process/at-red-dsl.md`
- `docs/atdd/process/at-red-system-driver.md`
- `docs/atdd/process/at-red-test.md`
- `docs/atdd/process/ct-cycle-conventions.md`
- `docs/atdd/process/ct-green-stubs.md`
- `docs/atdd/process/ct-red-dsl.md`
- `docs/atdd/process/ct-red-external-driver.md`
- `docs/atdd/process/ct-red-test.md`
- `docs/atdd/process/cycles.md`
- `docs/atdd/process/glossary.md`
- `docs/atdd/process/shared-commit-confirmation.md`
- `docs/atdd/process/shared-phase-progression.md`
- `docs/atdd/process/shared-ticket-status-in-acceptance.md`
- `docs/atdd/process/task-and-chore-cycles.md`

Per-phase WRITE / STOP / COMMIT mechanics for each AT and CT phase are in [diagram-phase-details.md](diagram-phase-details.md).

## Overview

```mermaid
flowchart TD
    START([Start])
    INTAKE[Intake — see § Intake]
    GATE_LEGACY{Legacy Coverage<br/>section absent?}
    LEGACY[Legacy Coverage Cycle — see § Legacy Coverage Cycle]
    GATE_TYPE{Ticket type?}
    AT[AT Cycle — see § AT Cycle]
    SYSAPI[System API Task Cycle — see § Structural Cycle]
    SYSUI[System UI Task Cycle — see § Structural Cycle]
    EXTAPI[External API Task Cycle — see § External API Task Cycle]
    CHORE[Chore Cycle — see § Structural Cycle]
    INACC[TICKET STATUS - IN ACCEPTANCE]
    END([End])

    START --> INTAKE
    INTAKE --> GATE_LEGACY
    GATE_LEGACY -- Yes --> GATE_TYPE
    GATE_LEGACY -- No --> LEGACY
    LEGACY --> GATE_TYPE
    GATE_TYPE -- story / bug --> AT
    GATE_TYPE -- system-api-task --> SYSAPI
    GATE_TYPE -- system-ui-task --> SYSUI
    GATE_TYPE -- external-api-task --> EXTAPI
    GATE_TYPE -- chore --> CHORE
    AT --> INACC
    SYSAPI --> INACC
    SYSUI --> INACC
    EXTAPI --> INACC
    CHORE --> INACC
    INACC --> END
```

## Intake

```mermaid
flowchart TD
    START([Start])
    PICK[Pick top Ready ticket]
    MOVE[Move ticket to In Progress]
    CLASSIFY[Classify ticket]
    GATE_TYPE{Ticket type?}
    STORY[atdd-story]
    BUG[atdd-bug]
    TASK[atdd-task]
    CHORE_INTAKE[atdd-chore]
    STOP_INTAKE[STOP - HUMAN REVIEW — approve scenarios]
    OUT[To § Overview gates]

    START -- mode == specific_issue --> MOVE
    START -- mode == board --> PICK
    PICK --> MOVE
    MOVE --> CLASSIFY
    CLASSIFY --> GATE_TYPE
    GATE_TYPE -- story --> STORY
    GATE_TYPE -- bug --> BUG
    GATE_TYPE -- system-api-task / system-ui-task / external-api-task --> TASK
    GATE_TYPE -- chore --> CHORE_INTAKE
    STORY --> STOP_INTAKE
    BUG --> STOP_INTAKE
    TASK --> STOP_INTAKE
    CHORE_INTAKE --> STOP_INTAKE
    STOP_INTAKE --> OUT

    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_INTAKE humanReviewNode
```

## AT Cycle

Per-phase mechanics for AT - RED - TEST, AT - RED - DSL, AT - RED - SYSTEM DRIVER, and AT - GREEN - SYSTEM are in [diagram-phase-details.md](diagram-phase-details.md).

```mermaid
flowchart TD
    AT_RED_TEST[AT - RED - TEST]
    GATE_DSL{DSL Interface<br/>unchanged?}
    AT_RED_DSL[AT - RED - DSL]
    GATE_EXT{External Driver Interface<br/>unchanged?}
    CT_SUBPROCESS[Contract Test Sub-Process — see § Contract Test Sub-Process]
    GATE_SYS{System Driver Interface<br/>unchanged?}
    AT_RED_SYSTEM_DRIVER[AT - RED - SYSTEM DRIVER]
    AT_GREEN_SYSTEM[AT - GREEN - SYSTEM — see § AT - GREEN - SYSTEM]
    AT_END([End])

    AT_RED_TEST --> GATE_DSL
    GATE_DSL -- Yes --> AT_GREEN_SYSTEM
    GATE_DSL -- No --> AT_RED_DSL
    AT_RED_DSL --> GATE_EXT
    GATE_EXT -- Yes --> GATE_SYS
    GATE_EXT -- No --> CT_SUBPROCESS
    CT_SUBPROCESS --> GATE_SYS
    GATE_SYS -- Yes --> AT_GREEN_SYSTEM
    GATE_SYS -- No --> AT_RED_SYSTEM_DRIVER
    AT_RED_SYSTEM_DRIVER --> AT_GREEN_SYSTEM
    AT_GREEN_SYSTEM --> AT_END
```

## AT - GREEN - SYSTEM

Per-phase mechanics for the WRITE step are in [diagram-phase-details.md](diagram-phase-details.md). This subgraph shows the cycle-level orchestration across `atdd-backend`, `atdd-frontend`, the human-review STOP, and `atdd-release`.

```mermaid
flowchart TD
    BACKEND[AT - GREEN - SYSTEM - WRITE - backend]
    FRONTEND[AT - GREEN - SYSTEM - WRITE - frontend]
    STOP_REVIEW[STOP - HUMAN REVIEW — approve implementation]
    RELEASE[AT - GREEN - SYSTEM - COMMIT - atdd-release]
    GS_END([End])

    BACKEND --> FRONTEND
    FRONTEND --> STOP_REVIEW
    STOP_REVIEW --> RELEASE
    RELEASE --> GS_END

    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_REVIEW humanReviewNode
```

## Contract Test Sub-Process

Per-phase mechanics for CT - RED - TEST, CT - RED - DSL, CT - RED - EXTERNAL DRIVER, and CT - GREEN - STUBS are in [diagram-phase-details.md](diagram-phase-details.md).

```mermaid
flowchart TD
    ONBOARDING[External System Onboarding — see § External System Onboarding Sub-Process]
    CT_RED_TEST[CT - RED - TEST]
    GATE_DSL{DSL Interface<br/>unchanged?}
    CT_RED_DSL[CT - RED - DSL]
    GATE_EXT{External Driver Interface<br/>unchanged?}
    CT_RED_EXT[CT - RED - EXTERNAL DRIVER]
    CT_GREEN_STUBS[CT - GREEN - STUBS]
    CT_END([End])

    ONBOARDING --> CT_RED_TEST
    CT_RED_TEST --> GATE_DSL
    GATE_DSL -- Yes --> CT_GREEN_STUBS
    GATE_DSL -- No --> CT_RED_DSL
    CT_RED_DSL --> GATE_EXT
    GATE_EXT -- Yes --> CT_GREEN_STUBS
    GATE_EXT -- No --> CT_RED_EXT
    CT_RED_EXT --> CT_GREEN_STUBS
    CT_GREEN_STUBS --> CT_END
```

## External System Onboarding Sub-Process

```mermaid
flowchart TD
    GATE_DRIVER{External System Driver<br/>exists?}
    ONBOARD_END([End])
    GATE_INSTANCE{Test Instance<br/>accessible?}
    PROVISION[Provision dockerized stand-in]
    DEFINE_IFACE[Define minimal Driver interface]
    IMPL_DRIVER[Implement Driver impl for Smoke Test]
    WRITE_SMOKE[Write single Smoke Test]
    RUN_SMOKE[Run Smoke Test]
    GATE_SMOKE{Smoke Test<br/>passes?}
    ASK_SUPPORT[STOP - HUMAN REVIEW — ask user for support]
    STOP_REVIEW[STOP - HUMAN REVIEW — approve onboarding artifacts]
    COMMIT["COMMIT: External System Onboarding | name"]

    GATE_DRIVER -- Yes --> ONBOARD_END
    GATE_DRIVER -- No --> GATE_INSTANCE
    GATE_INSTANCE -- Yes --> DEFINE_IFACE
    GATE_INSTANCE -- No --> PROVISION
    PROVISION --> DEFINE_IFACE
    DEFINE_IFACE --> IMPL_DRIVER
    IMPL_DRIVER --> WRITE_SMOKE
    WRITE_SMOKE --> RUN_SMOKE
    RUN_SMOKE --> GATE_SMOKE
    GATE_SMOKE -- Yes --> STOP_REVIEW
    GATE_SMOKE -- No --> ASK_SUPPORT
    STOP_REVIEW --> COMMIT
    COMMIT --> ONBOARD_END

    classDef effortNode fill:#004085,stroke:#002752,stroke-width:2px,color:#ffffff
    class PROVISION,DEFINE_IFACE,IMPL_DRIVER,WRITE_SMOKE effortNode
    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_REVIEW,ASK_SUPPORT humanReviewNode
```

## Structural Cycle

Shared by System API Task Cycle (phase suffix `SYSTEM API REDESIGN`), System UI Task Cycle (phase suffix `SYSTEM UI REDESIGN`), and Chore Cycle (phase suffix `CHORE`). The cycle's WRITE step is owned by `atdd-task` for system-api / system-ui subtypes and by `atdd-chore` for chores.

```mermaid
flowchart TD
    WRITE["PHASE - WRITE"]
    STOP_REVIEW[STOP - HUMAN REVIEW — approve implementation]
    GATE_TEST{TEST mode?}
    COMPILE[Compile in-scope projects]
    SAMPLE[Run sample suite]
    DRIFT[Print drift warning if applicable]
    STOP_TEST[STOP - HUMAN REVIEW — review TEST results]
    ASK_COMMIT["Ask: Can I commit?"]
    COMMIT["COMMIT: Ticket | PHASE"]
    TICK[Tick checklist items]
    STRUCT_END([End])

    WRITE --> STOP_REVIEW
    STOP_REVIEW --> GATE_TEST
    GATE_TEST -- skip --> ASK_COMMIT
    GATE_TEST -- compile --> COMPILE
    GATE_TEST -- full --> COMPILE
    COMPILE -- compile --> DRIFT
    COMPILE -- full --> SAMPLE
    SAMPLE --> DRIFT
    DRIFT --> STOP_TEST
    STOP_TEST --> ASK_COMMIT
    ASK_COMMIT --> COMMIT
    COMMIT --> TICK
    TICK --> STRUCT_END

    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_REVIEW,STOP_TEST humanReviewNode
```

## External API Task Cycle

Thin wrapper — has no standalone WRITE / REVIEW / TEST / COMMIT; routes entirely through the Contract Test Sub-Process.

```mermaid
flowchart TD
    EXTAPI_CT[Contract Test Sub-Process — see § Contract Test Sub-Process]
    EXTAPI_END([End])

    EXTAPI_CT --> EXTAPI_END
```

## Legacy Coverage Cycle

Internal phases TBD per `glossary.md` ("The Legacy Coverage Cycle's own internal phases are TBD"). The placeholder is a single human-review STOP so the runtime can dispatch something sensible until the spec lands.

```mermaid
flowchart TD
    LEGACY_TBD[STOP - HUMAN REVIEW — Legacy Coverage Cycle TBD]
    LEGACY_END([End])

    LEGACY_TBD --> LEGACY_END

    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class LEGACY_TBD humanReviewNode
```
