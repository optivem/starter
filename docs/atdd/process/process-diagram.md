# Process Diagram

> Generated by the `diagram-generator` agent from the prose docs in `docs/atdd/process/`. Overwritten on every run — do not edit by hand; edit the source docs and regenerate.

## Source docs

- `docs/atdd/process/at-cycle-conventions.md`
- `docs/atdd/process/at-green-system.md`
- `docs/atdd/process/at-red-dsl.md`
- `docs/atdd/process/at-red-system-driver.md`
- `docs/atdd/process/at-red-test.md`
- `docs/atdd/process/commit-confirmation.md`
- `docs/atdd/process/ct-cycle-conventions.md`
- `docs/atdd/process/ct-green-stubs.md`
- `docs/atdd/process/ct-red-dsl.md`
- `docs/atdd/process/ct-red-external-driver.md`
- `docs/atdd/process/ct-red-test.md`
- `docs/atdd/process/cycles.md`
- `docs/atdd/process/glossary.md`
- `docs/atdd/process/phase-progression.md`
- `docs/atdd/process/task-and-chore-cycles.md`

Per-phase WRITE / STOP / COMMIT mechanics for AT - RED - TEST, AT - RED - DSL, AT - RED - SYSTEM DRIVER, AT - GREEN - SYSTEM, CT - RED - TEST, CT - RED - DSL, CT - RED - EXTERNAL DRIVER, and CT - GREEN - STUBS are in [phase-details-diagram.md](phase-details-diagram.md).

## Overview

```mermaid
flowchart TD
    INTAKE[Intake]
    LEGACY[Legacy Coverage Cycle]
    AT[AT Cycle]
    SYS_API[System API Task Cycle]
    SYS_UI[System UI Task Cycle]
    EXT_API[External API Task Cycle]
    CHORE[Chore Cycle]
    CT[Contract Test Sub-Process]
    ONBOARD[External System Onboarding Sub-Process]
    DONE([DONE])

    INTAKE --> LEGACY
    INTAKE --> AT
    INTAKE --> SYS_API
    INTAKE --> SYS_UI
    INTAKE --> EXT_API
    INTAKE --> CHORE
    LEGACY --> AT
    LEGACY --> SYS_API
    LEGACY --> SYS_UI
    LEGACY --> EXT_API
    LEGACY --> CHORE
    AT --> CT
    EXT_API --> CT
    CT --> ONBOARD
    AT --> DONE
    SYS_API --> DONE
    SYS_UI --> DONE
    EXT_API --> DONE
    CHORE --> DONE
```

## Intake

```mermaid
flowchart TD
    START([Ticket picked])
    CLASSIFY{Ticket type?}
    STORY[atdd-story — produce change-driven AC]
    BUG[atdd-bug — produce change-driven AC]
    T_API[atdd-task-system-api]
    T_UI[atdd-task-system-ui]
    T_EXT[atdd-task-external-api]
    CHORE_A[atdd-chore]
    LEGACY_OPT[Process optional Legacy Coverage section]
    STOP_INTAKE[STOP - HUMAN REVIEW — approve intake]
    GATE_LEGACY{Ticket has Legacy Coverage section?}
    GATE_CHANGE{Change-driven AC produced?}
    LEGACY_CYCLE[Legacy Coverage Cycle — see § Legacy Coverage Cycle]
    AT_CYCLE[AT Cycle — see § AT Cycle]
    SYS_API_CYCLE[System API Task Cycle — see § System API Task Cycle]
    SYS_UI_CYCLE[System UI Task Cycle — see § System UI Task Cycle]
    EXT_API_CYCLE[External API Task Cycle — see § External API Task Cycle]
    CHORE_CYCLE[Chore Cycle — see § Chore Cycle]

    START --> CLASSIFY
    CLASSIFY -->|story| STORY
    CLASSIFY -->|bug| BUG
    CLASSIFY -->|system-api-task| T_API
    CLASSIFY -->|system-ui-task| T_UI
    CLASSIFY -->|external-api-task| T_EXT
    CLASSIFY -->|chore| CHORE_A
    STORY --> LEGACY_OPT
    BUG --> LEGACY_OPT
    T_API --> LEGACY_OPT
    T_UI --> LEGACY_OPT
    T_EXT --> LEGACY_OPT
    CHORE_A --> LEGACY_OPT
    LEGACY_OPT --> STOP_INTAKE
    STOP_INTAKE --> GATE_LEGACY
    GATE_LEGACY -->|Yes| LEGACY_CYCLE
    GATE_LEGACY -->|No| GATE_CHANGE
    LEGACY_CYCLE --> GATE_CHANGE
    GATE_CHANGE -->|Yes story/bug| AT_CYCLE
    GATE_CHANGE -->|No system-api-task| SYS_API_CYCLE
    GATE_CHANGE -->|No system-ui-task| SYS_UI_CYCLE
    GATE_CHANGE -->|No external-api-task| EXT_API_CYCLE
    GATE_CHANGE -->|No chore| CHORE_CYCLE

    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_INTAKE humanReviewNode
```

## AT Cycle

Per-phase mechanics for AT - RED - TEST, AT - RED - DSL, AT - RED - SYSTEM DRIVER, AT - GREEN - SYSTEM are in [phase-details-diagram.md](phase-details-diagram.md).

```mermaid
flowchart TD
    AT_RED_TEST[AT - RED - TEST]
    DSL_CHANGED{DSL Interface Changed?}
    AT_RED_DSL[AT - RED - DSL]
    EXT_DRIVER_CHANGED{External System Driver Interface Changed?}
    CT_SUBPROCESS[Contract Test Sub-Process — see § Contract Test Sub-Process]
    SYS_DRIVER_CHANGED{System Driver Interface Changed?}
    AT_RED_SYSTEM_DRIVER[AT - RED - SYSTEM DRIVER]
    AT_GREEN_SYSTEM[AT - GREEN - SYSTEM]
    DONE([DONE])

    AT_RED_TEST --> DSL_CHANGED
    DSL_CHANGED -->|No| AT_GREEN_SYSTEM
    DSL_CHANGED -->|Yes| AT_RED_DSL
    AT_RED_DSL --> EXT_DRIVER_CHANGED
    EXT_DRIVER_CHANGED -->|Yes| CT_SUBPROCESS
    CT_SUBPROCESS --> SYS_DRIVER_CHANGED
    EXT_DRIVER_CHANGED -->|No| SYS_DRIVER_CHANGED
    SYS_DRIVER_CHANGED -->|No| AT_GREEN_SYSTEM
    SYS_DRIVER_CHANGED -->|Yes| AT_RED_SYSTEM_DRIVER
    AT_RED_SYSTEM_DRIVER --> AT_GREEN_SYSTEM
    AT_GREEN_SYSTEM --> DONE
```

## Contract Test Sub-Process

Per-phase mechanics for CT - RED - TEST, CT - RED - DSL, CT - RED - EXTERNAL DRIVER, CT - GREEN - STUBS are in [phase-details-diagram.md](phase-details-diagram.md).

```mermaid
flowchart TD
    ONBOARD[External System Onboarding Sub-Process — see § External System Onboarding Sub-Process]
    CT_RED_TEST[CT - RED - TEST]
    DSL_CHANGED{DSL Interface Changed?}
    CT_RED_DSL[CT - RED - DSL]
    EXT_DRIVER_CHANGED{External System Driver Interface Changed?}
    CT_RED_EXT_DRIVER[CT - RED - EXTERNAL DRIVER]
    CT_GREEN_STUBS[CT - GREEN - STUBS]
    RETURN([Return to AT Cycle])

    ONBOARD --> CT_RED_TEST
    CT_RED_TEST --> DSL_CHANGED
    DSL_CHANGED -->|No| CT_GREEN_STUBS
    DSL_CHANGED -->|Yes| CT_RED_DSL
    CT_RED_DSL --> EXT_DRIVER_CHANGED
    EXT_DRIVER_CHANGED -->|No| CT_GREEN_STUBS
    EXT_DRIVER_CHANGED -->|Yes| CT_RED_EXT_DRIVER
    CT_RED_EXT_DRIVER --> CT_GREEN_STUBS
    CT_GREEN_STUBS --> RETURN
```

## External System Onboarding Sub-Process

```mermaid
flowchart TD
    START([Enter Onboarding])
    DRIVER_EXISTS{External System Driver exists?}
    RETURN_EARLY([Return to CT Sub-Process])
    INSTANCE_ACCESSIBLE{Test Instance accessible?}
    PROVISION[Provision dockerized stand-in]
    DEFINE_IFACE[Define minimal Driver interface]
    IMPL_DRIVER[Implement Driver impl for one Smoke Test]
    WRITE_SMOKE[Write single Smoke Test]
    RUN_SMOKE[Run Smoke Test]
    SMOKE_PASS{Smoke Test passes?}
    ASK_STOP[Ask user for support and STOP]
    STOP_REVIEW[STOP - HUMAN REVIEW — approve stand-in, Driver, Smoke Test]
    COMMIT[COMMIT: External System Onboarding]
    RETURN([Return to CT Sub-Process at CT - RED - TEST])

    START --> DRIVER_EXISTS
    DRIVER_EXISTS -->|Yes| RETURN_EARLY
    DRIVER_EXISTS -->|No| INSTANCE_ACCESSIBLE
    INSTANCE_ACCESSIBLE -->|Yes| DEFINE_IFACE
    INSTANCE_ACCESSIBLE -->|No| PROVISION
    PROVISION --> DEFINE_IFACE
    DEFINE_IFACE --> IMPL_DRIVER
    IMPL_DRIVER --> WRITE_SMOKE
    WRITE_SMOKE --> RUN_SMOKE
    RUN_SMOKE --> SMOKE_PASS
    SMOKE_PASS -->|No| ASK_STOP
    SMOKE_PASS -->|Yes| STOP_REVIEW
    STOP_REVIEW --> COMMIT
    COMMIT --> RETURN

    classDef effortNode fill:#cce5ff,stroke:#004085,stroke-width:2px
    class PROVISION,DEFINE_IFACE,IMPL_DRIVER,WRITE_SMOKE effortNode
    classDef humanReviewNode fill:#ffeb3b,stroke:#fbc02d,stroke-width:2px,color:#000
    class STOP_REVIEW humanReviewNode
```

## System API Task Cycle

```mermaid
flowchart TD
    START([Triggered: ticket type = system-api-task])
    UPDATE[Update System API Driver interface plus impl]
    STOP_REVIEW[STOP - HUMAN REVIEW — approve driver changes]
    COMMIT[COMMIT: Ticket SYSTEM API REDESIGN]
    WAIT[Wait for Pipeline - Acceptance Stage]
    PASS{Acceptance Stage passes?}
    FIX[Fix breakage]
    TICK[Tick checklist; if all ticked move issue to DONE]
    DONE([DONE])

    START --> UPDATE
    UPDATE --> STOP_REVIEW
    STOP_REVIEW --> COMMIT
    COMMIT --> WAIT
    WAIT --> PASS
    PASS -->|Yes| TICK
    TICK --> DONE
    PASS -->|No| FIX
    FIX --> WAIT
```

## System UI Task Cycle

```mermaid
flowchart TD
    START([Triggered: ticket type = system-ui-task])
    UPDATE[Update System UI Driver interface plus impl]
    STOP_REVIEW[STOP - HUMAN REVIEW — approve driver changes]
    COMMIT[COMMIT: Ticket SYSTEM UI REDESIGN]
    WAIT[Wait for Pipeline - Acceptance Stage]
    PASS{Acceptance Stage passes?}
    FIX[Fix breakage]
    TICK[Tick checklist; if all ticked move issue to DONE]
    DONE([DONE])

    START --> UPDATE
    UPDATE --> STOP_REVIEW
    STOP_REVIEW --> COMMIT
    COMMIT --> WAIT
    WAIT --> PASS
    PASS -->|Yes| TICK
    TICK --> DONE
    PASS -->|No| FIX
    FIX --> WAIT
```

## External API Task Cycle

```mermaid
flowchart TD
    START([Triggered: ticket type = external-api-task])
    CT[Contract Test Sub-Process — see § Contract Test Sub-Process]
    WAIT[Wait for Pipeline - Acceptance Stage]
    PASS{Acceptance Stage passes?}
    FIX[Fix breakage]
    TICK[Tick checklist; if all ticked move issue to DONE]
    DONE([DONE])

    START --> CT
    CT --> WAIT
    WAIT --> PASS
    PASS -->|Yes| TICK
    TICK --> DONE
    PASS -->|No| FIX
    FIX --> WAIT
```

## Chore Cycle

```mermaid
flowchart TD
    START([Triggered: ticket type = chore])
    IMPL[Implement chore — refactor / upgrade / rename]
    STOP_REVIEW[STOP - HUMAN REVIEW — approve implementation]
    COMMIT[COMMIT: Ticket CHORE]
    WAIT[Wait for Pipeline - Acceptance Stage]
    PASS{Acceptance Stage passes?}
    FIX[Fix breakage]
    TICK[Tick checklist; if all ticked move issue to DONE]
    DONE([DONE])

    START --> IMPL
    IMPL --> STOP_REVIEW
    STOP_REVIEW --> COMMIT
    COMMIT --> WAIT
    WAIT --> PASS
    PASS -->|Yes| TICK
    TICK --> DONE
    PASS -->|No| FIX
    FIX --> WAIT
```

## Legacy Coverage Cycle

```mermaid
flowchart TD
    START([Triggered: ticket has Legacy Coverage section])
    NOTE[Test-last retroactive AC — internal phases TBD; see glossary.md]
    NEXT([Continue to ticket-type cycle])

    START --> NOTE
    NOTE --> NEXT
```

## Notes

- The Legacy Coverage Cycle's internal phases are explicitly marked TBD in `cycles.md` and `glossary.md`; the cycle is therefore drawn as a single placeholder node rather than a fabricated phase sequence.
