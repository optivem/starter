# Architecture Diagram

> Generated by the `diagram-generator` agent from the prose docs in `docs/atdd/architecture/`. Overwritten on every run — do not edit by hand; edit the source docs and regenerate.

## Source docs

- `docs/atdd/architecture/driver-adapter.md`
- `docs/atdd/architecture/driver-port.md`
- `docs/atdd/architecture/dsl-core.md`
- `docs/atdd/architecture/dsl-port.md`
- `docs/atdd/architecture/system.md`
- `docs/atdd/architecture/test.md`

## Overview

```mermaid
flowchart TD
    TEST[Test Files]
    DSL_PORT[DSL Port — see § DSL Port]
    DSL_CORE[DSL Core — see § DSL Core]
    DRIVER_PORT[Driver Port — see § Driver Port]
    DRIVER_ADAPTER[Driver Adapter — see § Driver Adapter]
    SYSTEM[System — see § System]
    EXTERNAL[External Systems]

    TEST --> DSL_PORT
    DSL_PORT --> DSL_CORE
    DSL_CORE --> DRIVER_PORT
    DRIVER_PORT --> DRIVER_ADAPTER
    DRIVER_ADAPTER --> SYSTEM
    DRIVER_ADAPTER --> EXTERNAL
```

## Test

```mermaid
flowchart TD
    POSITIVE[UseCasePositiveTest]
    NEGATIVE[UseCaseNegativeTest]
    SHOULD_SUCCEED[shouldSucceed assertion]
    SHOULD_FAIL[shouldFailWith assertion]
    DSL_PORT[DSL Port — see § DSL Port]

    POSITIVE --> SHOULD_SUCCEED
    NEGATIVE --> SHOULD_FAIL
    POSITIVE --> DSL_PORT
    NEGATIVE --> DSL_PORT
```

## DSL Port

```mermaid
flowchart TD
    GIVEN_STAGE[GivenStage]
    WHEN_STAGE[WhenStage]
    THEN_STAGE[ThenStage]
    THEN_RESULT[ThenResultStage]
    THEN_STEP[ThenStep TThen]
    THEN_SUCCESS[ThenSuccess]
    THEN_FAILURE[ThenFailure]
    WITH_STRING[withFieldName String]
    WITH_TYPED[withFieldName T typed]
    AND[and]
    WHEN[when]
    THEN[then]

    GIVEN_STAGE -->|when| WHEN_STAGE
    GIVEN_STAGE -->|then| THEN_STAGE
    WHEN_STAGE -->|then| THEN_STAGE
    THEN_RESULT --> THEN_STAGE
    THEN_RESULT -->|shouldSucceed| THEN_SUCCESS
    THEN_RESULT -->|shouldFail| THEN_FAILURE
    THEN_STEP --> THEN_SUCCESS
    THEN_STEP --> THEN_FAILURE
    GIVEN_STAGE --- WITH_STRING
    GIVEN_STAGE --- WITH_TYPED
    GIVEN_STAGE --- AND
    GIVEN_STAGE --- WHEN
    GIVEN_STAGE --- THEN
```

## DSL Core

```mermaid
flowchart TD
    GIVEN_IMPL[Given Step Class]
    WHEN_IMPL[When Step Class]
    THEN_IMPL[Then Step Class]
    VERIFICATION[Verification Class]
    WITH_METHOD[withFieldName method]
    ALIAS_FIELD[Alias Field]
    USECASE[Use Case Class]
    DSL_PORT[DSL Port — see § DSL Port]
    DRIVER_PORT[Driver Port — see § Driver Port]

    DSL_PORT --> GIVEN_IMPL
    DSL_PORT --> WHEN_IMPL
    DSL_PORT --> THEN_IMPL
    GIVEN_IMPL --> WITH_METHOD
    WHEN_IMPL --> WITH_METHOD
    WHEN_IMPL --> ALIAS_FIELD
    GIVEN_IMPL --> USECASE
    WHEN_IMPL --> USECASE
    THEN_IMPL --> VERIFICATION
    USECASE --> DRIVER_PORT
```

## Driver Port

```mermaid
flowchart TD
    SHOP_PKG[shop package]
    EXTERNAL_PKG[external package]
    SHOP_API_IFACE[Shop API Driver Interface]
    SHOP_UI_IFACE[Shop UI Driver Interface]
    EXT_CLOCK_IFACE[external/clock Interface]
    EXT_ERP_IFACE[external/erp Interface]
    EXT_TAX_IFACE[external/tax Interface]
    REQUEST_DTO[Request DTO]
    RESPONSE_DTO[Response DTO]
    DRIVER_ADAPTER[Driver Adapter — see § Driver Adapter]

    SHOP_PKG --> SHOP_API_IFACE
    SHOP_PKG --> SHOP_UI_IFACE
    EXTERNAL_PKG --> EXT_CLOCK_IFACE
    EXTERNAL_PKG --> EXT_ERP_IFACE
    EXTERNAL_PKG --> EXT_TAX_IFACE
    SHOP_API_IFACE --> REQUEST_DTO
    SHOP_API_IFACE --> RESPONSE_DTO
    EXT_ERP_IFACE --> REQUEST_DTO
    EXT_ERP_IFACE --> RESPONSE_DTO
    SHOP_API_IFACE --> DRIVER_ADAPTER
    SHOP_UI_IFACE --> DRIVER_ADAPTER
    EXT_CLOCK_IFACE --> DRIVER_ADAPTER
    EXT_ERP_IFACE --> DRIVER_ADAPTER
    EXT_TAX_IFACE --> DRIVER_ADAPTER
```

## Driver Adapter — Shop Drivers

```mermaid
flowchart TD
    SHOP_API_DRIVER[Shop API Driver]
    SHOP_UI_DRIVER[Shop UI Driver]
    SHOP_API_CLIENT[ShopApiClient]
    ORDER_CTRL[OrderController]
    COUPON_CTRL[CouponController]
    PRODUCT_CTRL[ProductController]
    URL_CONST[Endpoint URL Constant]
    PROBLEM_DETAIL[ProblemDetailResponse]
    ERROR_MAPPER[SystemErrorMapper]
    PAGE_OBJECT[Page Object aria-label]
    GO_TO[goToShop health check]
    DRIVER_PORT[Driver Port — see § Driver Port]
    SYSTEM[System — see § System]

    DRIVER_PORT --> SHOP_API_DRIVER
    DRIVER_PORT --> SHOP_UI_DRIVER
    SHOP_API_DRIVER --> SHOP_API_CLIENT
    SHOP_API_CLIENT --> ORDER_CTRL
    SHOP_API_CLIENT --> COUPON_CTRL
    SHOP_API_CLIENT --> PRODUCT_CTRL
    ORDER_CTRL --> URL_CONST
    PROBLEM_DETAIL --> ERROR_MAPPER
    SHOP_API_DRIVER --> GO_TO
    SHOP_UI_DRIVER --> PAGE_OBJECT
    URL_CONST --> SYSTEM
    PAGE_OBJECT --> SYSTEM
```

## Driver Adapter — External Drivers

```mermaid
flowchart TD
    EXT_REAL[XyzRealDriver]
    EXT_STUB[XyzStubDriver]
    BASE_DRIVER[BaseXyzDriver]
    BASE_CLIENT[BaseXyzClient]
    REAL_CLIENT[XyzRealClient]
    STUB_CLIENT[XyzStubClient]
    EXT_REQUEST[Ext*Request]
    EXT_RESPONSE[Ext*Response]
    GO_TO[goToErp health check]
    DRIVER_PORT[Driver Port — see § Driver Port]
    EXTERNAL[External Systems]

    DRIVER_PORT --> EXT_REAL
    DRIVER_PORT --> EXT_STUB
    EXT_REAL --> BASE_DRIVER
    EXT_STUB --> BASE_DRIVER
    BASE_DRIVER --> BASE_CLIENT
    REAL_CLIENT --> BASE_CLIENT
    STUB_CLIENT --> BASE_CLIENT
    EXT_REAL --> EXT_REQUEST
    EXT_REAL --> EXT_RESPONSE
    EXT_STUB --> EXT_REQUEST
    EXT_STUB --> EXT_RESPONSE
    EXT_REAL --> GO_TO
    REAL_CLIENT --> EXTERNAL
    STUB_CLIENT --> EXTERNAL
```

## System

```mermaid
flowchart TD
    MONO_JAVA[system/monolith/java]
    MONO_DOTNET[system/monolith/dotnet]
    MONO_TS[system/monolith/typescript]
    MULTI_BACK_JAVA[system/multitier/backend-java]
    MULTI_BACK_DOTNET[system/multitier/backend-dotnet]
    MULTI_BACK_TS[system/multitier/backend-typescript]
    MULTI_FRONT[system/multitier/frontend-react]
    BACKEND_ROUTE[Backend Route]
    UI_FETCH[UI Fetch Site]
    DRIVER_CONST[Driver Adapter URL Constant]
    LEGACY[system-test Legacy read-only]

    MONO_JAVA --> BACKEND_ROUTE
    MONO_DOTNET --> BACKEND_ROUTE
    MONO_TS --> BACKEND_ROUTE
    MULTI_BACK_JAVA --> BACKEND_ROUTE
    MULTI_BACK_DOTNET --> BACKEND_ROUTE
    MULTI_BACK_TS --> BACKEND_ROUTE
    MONO_JAVA --> UI_FETCH
    MONO_DOTNET --> UI_FETCH
    MONO_TS --> UI_FETCH
    MULTI_FRONT --> UI_FETCH
    BACKEND_ROUTE --- DRIVER_CONST
    UI_FETCH --- DRIVER_CONST
```
