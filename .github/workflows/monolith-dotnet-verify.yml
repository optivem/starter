name: monolith-dotnet-verify

on:
  workflow_dispatch:
    inputs:
      skip-commit-stage:
        description: 'Skip commit stage'
        required: false
        type: boolean
        default: false

jobs:
  verify:
    uses: ./.github/workflows/_verify-pipeline.yml
    secrets: inherit
    with:
      prefix: monolith-dotnet
      commit-workflows: '["monolith-dotnet-commit-stage.yml"]'
      skip-commit-stage: ${{ inputs.skip-commit-stage == true }}
