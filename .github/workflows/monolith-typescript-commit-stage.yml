name: monolith-typescript-commit-stage

on:
  push:
    branches: [ "main" ]
    paths:
        - 'system/monolith/typescript/**'
        - '.github/workflows/monolith-typescript-commit-stage.yml'
  pull_request:
    branches: [ "main" ]
    paths:
        - 'system/monolith/typescript/**'
        - '.github/workflows/monolith-typescript-commit-stage.yml'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Validate Config
        uses: optivem/actions/validate-config@v1
        with:
          names: |
            DOCKERHUB_USERNAME
            DOCKERHUB_TOKEN
        env:
          DOCKERHUB_USERNAME: ${{ vars.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Check Target Version Exists
        uses: optivem/actions/check-target-version-exists@v1
        with:
          file: system/monolith/typescript/VERSION

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

  run:
    needs: check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    outputs:
      image-digest-url: ${{ steps.push.outputs.image-digest-url }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Setup Runtime
        uses: optivem/actions/setup-node@v1
        with:
          node-version: 22.x
          working-directory: system/monolith/typescript

      - name: Compile Code
        run: npm run build
        working-directory: system/monolith/typescript

      # TODO: Implement
      - name: Run Unit Tests
        run: echo "TODO - not yet implemented"

      # TODO: Implement
      - name: Run Narrow Integration Tests
        run: echo "TODO - not yet implemented"

      # TODO: Implement
      - name: Run Component Tests
        run: echo "TODO - not yet implemented"

      # TODO: Implement
      - name: Run Contract Tests
        run: echo "TODO - not yet implemented"

      - name: Run Linter
        run: npm run lint
        working-directory: system/monolith/typescript

      - name: Run Code Analysis
        if: github.ref == 'refs/heads/main' && env.SONAR_TOKEN != ''
        uses: SonarSource/sonarcloud-github-action@v5
        with:
          projectBaseDir: system/monolith/typescript
          args: >
            -Dsonar.projectKey=optivem_starter-monolith-typescript
            -Dsonar.organization=optivem
            -Dsonar.sources=src

      - name: Read Target Version
        if: github.ref == 'refs/heads/main'
        id: read-version
        uses: optivem/actions/read-target-version@v1
        with:
          file: system/monolith/typescript/VERSION

      - name: Build Artifact
        if: github.ref == 'refs/heads/main'
        id: build
        uses: optivem/actions/build-docker-image@v1
        with:
          working-directory: system/monolith/typescript
          image-name: monolith-system-typescript
          image-namespace: ${{ github.repository }}
          commit-sha: ${{ github.sha }}
          dockerfile: Dockerfile
          component-version: ${{ steps.read-version.outputs.target-version }}
          build-number: ${{ github.run_number }}

      - name: Push Artifact
        if: github.ref == 'refs/heads/main'
        id: push
        uses: optivem/actions/push-docker-image@v1
        with:
          image-sha-tag: ${{ steps.build.outputs.image-sha-tag }}
          image-latest-url: ${{ steps.build.outputs.image-latest-url }}
          image-version-tag: ${{ steps.build.outputs.image-version-tag }}
          registry-username: ${{ github.actor }}
        env:
          REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}

  summary:
    needs: [check, run]
    if: always()
    runs-on: ubuntu-latest
    steps:
    - name: Summarize Stage
      uses: optivem/actions/summarize-commit-stage@v1
      with:
        stage-result: ${{ needs.run.result }}
        success-artifact-url: ${{ needs.run.outputs.image-digest-url }}
