name: multitier-system-java-acceptance-stage

on:
  schedule:
    - cron: '0 * * * *'  # Run every hour
  workflow_dispatch:

concurrency:
  group: multitier-system-java-acceptance-stage

jobs:
  preflight:
    runs-on: ubuntu-latest
    permissions:
      packages: read
    outputs:
      artifacts-exist: ${{ steps.check-artifacts.outputs.exist }}
    steps:
      - name: Check Artifacts Exist
        id: check-artifacts
        uses: optivem/actions/check-artifacts-exist@v1

  check:
    needs: preflight
    if: needs.preflight.outputs.artifacts-exist == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      should-run: ${{ steps.should-run.outputs.should-run }}
      image-digest-urls: ${{ steps.find-latest.outputs.image-digest-urls }}
    steps:
      - name: Validate Config
        uses: optivem/actions/validate-config@v1
        with:
          names: |
            DOCKERHUB_USERNAME
            DOCKERHUB_TOKEN
        env:
          DOCKERHUB_USERNAME: ${{ vars.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Checkout Repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Check Target Version Exists
        uses: optivem/actions/check-target-version-exists@v1

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Find Latest Artifacts
        id: find-latest
        uses: optivem/actions/find-latest-docker-images@v1
        with:
          image-urls: |
            ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}/multitier-frontend-react:latest
            ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}/multitier-backend-java:latest

      - name: Check Run Conditions
        id: should-run
        uses: optivem/actions/should-run-acceptance-stage@v1
        with:
          latest-image-timestamp: ${{ steps.find-latest.outputs.latest-image-timestamp }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  run:
    needs: check
    if: needs.check.outputs.should-run == 'true'
    runs-on: ubuntu-latest
    environment: multitier-java-acceptance
    permissions:
      contents: write
      packages: write
    outputs:
      version: ${{ steps.promote-to-rc.outputs.version }}
      image-urls: ${{ steps.promote-to-rc.outputs.image-urls }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Deploy System (Acceptance Environment)
        id: deploy-system
        uses: optivem/actions/simulate-deployment@v1
        with:
          environment: acceptance
          version: latest
          image-urls: ${{ needs.check.outputs.image-digest-urls }}
          compose-file: docker-compose.pipeline.multitier.real.yml
          systems: |
            [
              { "name": "Shop UI", "url": "${{ vars.SYSTEM_UI_URL }}/" },
              { "name": "Shop API", "url": "${{ vars.SYSTEM_API_URL }}/health" },
              { "name": "ERP", "url": "${{ vars.ERP_URL }}/health" },
              { "name": "Clock", "url": "${{ vars.CLOCK_URL }}/health" }
            ]
          working-directory: system-test/java

      - name: Setup Runtime
        uses: optivem/actions/setup-java-gradle@v1
        with:
          java-version: 21
          working-directory: system-test/java

      - name: Smoke (Stub)
        run: ./gradlew test -Dtype=smoke -DexternalSystemMode=stub -Denvironment=acceptance
        working-directory: system-test/java

      - name: Smoke (Real)
        run: ./gradlew test -Dtype=smoke -DexternalSystemMode=real -Denvironment=acceptance
        working-directory: system-test/java

      - name: Acceptance - API Channel
        run: ./gradlew test -Dtype=acceptance -DexcludeTags=isolated -Dchannel=API -Denvironment=acceptance
        working-directory: system-test/java

      - name: Acceptance - UI Channel
        run: ./gradlew test -Dtype=acceptance -DexcludeTags=isolated -Dchannel=UI -Denvironment=acceptance
        working-directory: system-test/java

      - name: Acceptance (Isolated) - API Channel
        run: ./gradlew test -Dtype=acceptance -DincludeTags=isolated -Dchannel=API -Denvironment=acceptance
        working-directory: system-test/java

      - name: Acceptance (Isolated) - UI Channel
        run: ./gradlew test -Dtype=acceptance -DincludeTags=isolated -Dchannel=UI -Denvironment=acceptance
        working-directory: system-test/java

      - name: Contract (Stub)
        run: ./gradlew test -Dtype=contract -DexternalSystemMode=stub -Denvironment=acceptance
        working-directory: system-test/java

      - name: Contract (Real)
        run: ./gradlew test -Dtype=contract -DexternalSystemMode=real -Denvironment=acceptance
        working-directory: system-test/java

      - name: E2E - API Channel
        run: ./gradlew test -Dtype=e2e -Dchannel=API -Denvironment=acceptance
        working-directory: system-test/java

      - name: E2E - UI Channel
        run: ./gradlew test -Dtype=e2e -Dchannel=UI -Denvironment=acceptance
        working-directory: system-test/java

      - name: Read Target Version
        id: read-version
        uses: optivem/actions/read-target-version@v1

      - name: Promote to RC
        id: promote-to-rc
        uses: optivem/actions/promote-to-rc@v1
        with:
          target-version: ${{ steps.read-version.outputs.target-version }}
          image-urls: ${{ needs.check.outputs.image-digest-urls }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  summary:
    needs: [check, run]
    if: always()
    runs-on: ubuntu-latest
    steps:
    - name: Summarize Stage
      uses: optivem/actions/summarize-system-stage@v1
      with:
        stage-name: 'Acceptance Stage'
        environment: acceptance
        stage-result: ${{ needs.run.result }}
        success-version: ${{ needs.run.outputs.version }}
        success-artifact-ids: ${{ needs.run.outputs.image-urls }}
