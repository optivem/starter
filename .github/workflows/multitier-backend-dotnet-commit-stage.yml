name: multitier-backend-dotnet-commit-stage

on:
  push:
    branches: [ "main" ]
    paths:
        - 'system/multitier/backend-dotnet/**'
        - '.github/workflows/multitier-backend-dotnet-commit-stage.yml'
  pull_request:
    branches: [ "main" ]
    paths:
        - 'system/multitier/backend-dotnet/**'
        - '.github/workflows/multitier-backend-dotnet-commit-stage.yml'
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git SHA to build (leave empty for HEAD)'
        required: false
        type: string

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Ensure Environment Variables Defined
        uses: optivem/actions/ensure-env-vars-defined@v1
        with:
          names: |
            DOCKERHUB_USERNAME
            DOCKERHUB_TOKEN
        env:
          DOCKERHUB_USERNAME: ${{ vars.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Checkout Repository
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.ref || github.sha }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

  run:
    needs: check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    outputs:
      component-version: ${{ steps.read-version.outputs.target-version }}
      image-version-tag: ${{ fromJSON(steps.tag.outputs.image-urls)[2] }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.ref || github.sha }}

      - name: Read Component Target Version
        id: read-version
        uses: optivem/actions/read-target-version@v1
        with:
          file: system/multitier/backend-dotnet/VERSION

      - name: Setup .NET
        uses: optivem/actions/setup-dotnet@v1
        with:
          dotnet-version: 8.0.x

      - name: Compile Code
        run: dotnet build
        working-directory: system/multitier/backend-dotnet

      # TODO: Implement
      - name: Run Unit Tests
        run: echo "TODO - not yet implemented"

      # TODO: Implement
      - name: Run Narrow Integration Tests
        run: echo "TODO - not yet implemented"

      # TODO: Implement
      - name: Run Component Tests
        run: echo "TODO - not yet implemented"

      # TODO: Implement
      - name: Run Contract Tests
        run: echo "TODO - not yet implemented"

      - name: Run Linter
        run: dotnet format Optivem.Shop.Backend.slnx --verify-no-changes
        working-directory: system/multitier/backend-dotnet

      - name: Run Code Analysis
        if: github.ref == 'refs/heads/main' && env.SONAR_TOKEN != ''
        run: |
          dotnet tool install --global dotnet-sonarscanner
          dotnet sonarscanner begin /k:"optivem_shop-multitier-backend-dotnet" /n:"shop-multitier-backend-dotnet" /o:"optivem" /d:sonar.token="${SONAR_TOKEN}" /d:sonar.issue.ignore.multicriteria=e1,e2,e3,e4,e5 /d:sonar.issue.ignore.multicriteria.e1.ruleKey=csharpsquid:S2699 /d:sonar.issue.ignore.multicriteria.e1.resourceKey="Tests/**" /d:sonar.issue.ignore.multicriteria.e2.ruleKey=csharpsquid:S1118 /d:sonar.issue.ignore.multicriteria.e2.resourceKey="Program.cs" /d:sonar.issue.ignore.multicriteria.e3.ruleKey=csharpsquid:S2068 /d:sonar.issue.ignore.multicriteria.e3.resourceKey="appsettings.json" /d:sonar.issue.ignore.multicriteria.e4.ruleKey=csharpsquid:S1075 /d:sonar.issue.ignore.multicriteria.e4.resourceKey="**/*" /d:sonar.issue.ignore.multicriteria.e5.ruleKey=csharpsquid:S3267 /d:sonar.issue.ignore.multicriteria.e5.resourceKey="**/*"
          dotnet build
          dotnet sonarscanner end /d:sonar.token="${SONAR_TOKEN}"
        working-directory: system/multitier/backend-dotnet

      - name: Build Artifact
        if: github.ref == 'refs/heads/main'
        id: build
        uses: optivem/actions/build-docker-image@v1
        with:
          working-directory: system/multitier/backend-dotnet
          image-name: multitier-backend-dotnet
          commit-sha: ${{ inputs.ref || github.sha }}
          dockerfile: Dockerfile

      - name: Compose Dev Version
        if: github.ref == 'refs/heads/main'
        id: dev-version
        uses: optivem/actions/compose-prerelease-version@v1
        with:
          target-version: ${{ steps.read-version.outputs.target-version }}
          suffix: dev
          number: ${{ github.run_number }}

      - name: Tag Docker Image
        if: github.ref == 'refs/heads/main'
        id: tag
        uses: optivem/actions/tag-docker-image-local@v1
        with:
          source: ${{ steps.build.outputs.local-image-ref }}
          image-name: multitier-backend-dotnet
          tags: |
            ${{ inputs.ref || github.sha }}
            latest
            ${{ steps.dev-version.outputs.version }}

      - name: Push Artifact
        if: github.ref == 'refs/heads/main'
        id: push
        uses: optivem/actions/push-docker-image@v1
        with:
          image-sha-tag: ${{ fromJSON(steps.tag.outputs.image-urls)[0] }}
          image-latest-url: ${{ fromJSON(steps.tag.outputs.image-urls)[1] }}
          image-version-tag: ${{ fromJSON(steps.tag.outputs.image-urls)[2] }}

  summary:
    needs: [check, run]
    if: always()
    runs-on: ubuntu-latest
    steps:
    - name: Summarize Stage
      uses: optivem/actions/compose-commit-stage-summary@v1
      with:
        stage-result: ${{ needs.run.result }}
        success-artifact-url: ${{ needs.run.outputs.image-version-tag }}
